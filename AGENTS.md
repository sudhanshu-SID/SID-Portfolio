## Agent skills

### Issue tracker

GitHub issues (uses the `gh` CLI). See `docs/agents/issue-tracker.md`.

### Triage labels

The default 5 canonical roles. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context repo structure (one `CONTEXT.md` at root). See `docs/agents/domain.md`.
